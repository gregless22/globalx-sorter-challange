package cli

import (
	"testing"
)

func TestNoArg(t *testing.T) {
	// is := is.New(t)

	// args := []string{}

	// ts := service{args}

	// err := service.checkArgs(args)
	// is.Equal(err.Error(), "no file added as a command line argument")

}

func TestArgEmpty(t *testing.T) {
	// is := is.New(t)

	// args := []string{""}
	// err := checkArgs(args)
	// is.Equal(err.Error(), "filename cannot be empty")
}
